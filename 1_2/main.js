const person = {
    name: "Sven",
    alter:29,
    sagNameAlter: () => {
        alert(person.name + " " + person.alter)
        console.log(person.name,person.alter)
    }
}
person.sagNameAlter()

